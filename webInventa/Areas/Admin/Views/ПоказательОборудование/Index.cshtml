@model IEnumerable<InventaDataModul.ПоказательОборудование>



    
    <div class="form-group">
        @Html.Label("Харрактеристики", htmlAttributes: new { @class = "control-label col-md-2" })

        @foreach (var item in Model)
        {
            <div class="col-md-10">
                @Html.DisplayFor(modelItem => item.Показатель.Представление)
            </div>
        }
    </div>
<p>
    @Html.ActionLink("Создать", "Create")
</p>

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Код_Оборудования)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Код_Показателя)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Код_Оборудования)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Код_Показателя)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Код }) |            
            @Html.ActionLink("Удалить", "Delete", new { id=item.Код })
        </td>
    </tr>
}

</table>*@
